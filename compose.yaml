services:
  db:
    image: mysql
    container_name: mysql
    environment:
      MYSQL_DATABASE: roombooking
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306
    volumes:
      - ./mysql:/var/lib/mysql

volumes:
  roombooking: